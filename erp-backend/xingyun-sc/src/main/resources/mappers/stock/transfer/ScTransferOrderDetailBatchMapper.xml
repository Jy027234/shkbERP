<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lframework.xingyun.sc.mappers.ScTransferOrderDetailBatchMapper">

    <update id="receiveBatch">
        UPDATE tbl_sc_transfer_order_detail_batch
        SET received_num = received_num + #{receiveNum}
        WHERE id = #{id}
          AND transfer_num - received_num &gt;= #{receiveNum}
    </update>
</mapper>
